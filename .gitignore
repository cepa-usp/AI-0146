# Ignora os arquivos gerados pelo FlashDevelop
/bin

# Ignora a versão anterior da configuração do projeto FlashDevelop
/obj/*.old

# Ignora qualquer pasta deploy: este não é repositório para isso
deploy/
